{
  "name": "salihonder",
  "version": "1.0.0",
  "private": true,
  "description": "Salih ÖNDER - WP / FlexibleGS Wordpress Starter Kit",
  "main": "gulpfile.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/salihonder05/wp-flexible-starter-template.git"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-cli": "^2.3.0",
    "gulp-sass": "^5.1.0",
    "gulp-sass-lint": "^1.4.0",
    "gulp-sourcemaps": "^3.0.0",
    "sass": "^1.59.3"
  },
  "keywords": [],
  "author": "Salih ÖNDER",
  "license": "ISC",
  "scripts": {
    "buildm": "npm install --unsafe-perm=true --allow-root && rm -f style.css && rm -f style.css.map && gulp build",
    "build": "npm install --unsafe-perm=true --allow-root && del -f style.css && del -f style.css.map && gulp build",
    "startm": "rm -f style.css && rm -f style.css.map && gulp",
    "start": "del -f style.css && del -f style.css.map && gulp",
    "lh": "lighthouse https://siteadi.com --budget-path=budget.json"
  },
  "browserslist": [
    "ff > 2",
    "> 2%",
    "ie 8"
  ]
}
